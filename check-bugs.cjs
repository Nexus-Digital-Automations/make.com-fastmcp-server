const TaskManager = require("/Users/jeremyparker/Desktop/Claude Coding Projects/infinite-continue-stop-hook/lib/taskManager"); const tm = new TaskManager("./TODO.json"); tm.readTodo().then(data => { const criticalBugs = data.tasks.filter(t => t.status === "pending" && t.category === "bug" && t.priority === "critical" && \!t.assigned_agent && \!t.claimed_by); criticalBugs.sort((a, b) => new Date(a.created_at).getTime() - new Date(b.created_at).getTime()); console.log("Available critical bugs (oldest first):"); criticalBugs.forEach(t => console.log(`- ${t.id}: ${t.title}`)); });
